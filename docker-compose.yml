version: '3'

volumes:
  pg_wb:

services:
  db:
    image: postgres:13.10
    env_file: .env
    volumes:
      - pg_wb:/var/lib/postgresql/data
  
  backend:
    build: 
      context: /Users/Panisher/Desktop/wb_tech/test/wb/
      dockerfile: Dockerfile
    env_file: /Users/Panisher/Desktop/wb_tech/test/.env
    depends_on:
      - db